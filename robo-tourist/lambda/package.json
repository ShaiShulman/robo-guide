{
  "name": "lambda",
  "version": "1.0.0",
  "description": "AWS Lambda getSuggestions function",
  "main": "index.js",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "postbuild": "cd dist && zip -r index.zip index.js*"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "@types/node": "^18.15.10",
    "esbuild": "^0.17.14"
  },
  "dependencies": {
    "node-fetch": "^2.6.9"
  }
}
